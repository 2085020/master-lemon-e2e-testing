name: CI workflow

on: pull_request

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout#v3

      - name: Install
        uses: npm ci

      - name: Test e22
        uses: npm run test:e2e:ci
